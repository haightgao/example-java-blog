spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/java_blog?useUnicode=true&characterEncoding=utf-8
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    cc.gaoht: debug
  file:
    name: log/blog-dev.log